#!/bin/bash
if [ $# -ne 1 ];then
 echo "WARNING: Please insert a URL for health check"
 exit 1
else
 CHECK_URL=$1
 CMD=$(curl -o /dev/null -s -w "%{http_code}\n" ${CHECK_URL} | grep "200" | wc -l)
 if [ ${CMD} -eq 1 ];then
   exit 0
 else
   exit 1
 fi
fi